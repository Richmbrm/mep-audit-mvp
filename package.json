{
  "name": "audit-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/gen-history.js",
    "build": "npm run prebuild && vite build",
    "preview": "vite preview",
    "server": "node server.js",
    "start": "node server.js"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1"
  }
}